"""Operational data adapters for BANDHAN.

The default adapter is file-backed so the system runs locally. Production
deployments can point each source at a REST endpoint through environment
variables without changing the scheduling layer.
"""
